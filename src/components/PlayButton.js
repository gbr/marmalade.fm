import React from 'react';

export default function PlayButton() {
    return (
        <div>
            <button className="bg-transparent contain button-reset play-button db bg-center pointer center" />
        </div>
    )
}